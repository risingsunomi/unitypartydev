import React from 'react';
import clsx from 'clsx';
import styles from './HomepageFeatures.module.css';

const FeatureList = [
  {
    title: 'Left Unity',
    Svg: require('../../static/img/hands2.svg').default,
    description: (
      <>
        Unity’s goal is to unite the left of the server from across the whole spectrum.
      </>
    ),
  },
  {
    title: 'Representation',
    Svg: require('../../static/img/representation.svg').default,
    description: (
      <>
        Unity guarentees representation in government for all peoples
      </>
    ),
  },
];

function Feature({Svg, title, description}) {
  return (
    <div className={clsx('col col--6')}>
      <div className="text--center">
        <Svg className={styles.featureSvg} alt={title} />
      </div>
      <div className="text--center padding-horiz--md">
        <h3>{title}</h3>
        <p>{description}</p>
      </div>
    </div>
  );
}

export default function HomepageFeatures() {
  return (
    <section className={styles.features}>
      <div className="container">
        <div className="row">
          {FeatureList.map((props, idx) => (
            <Feature key={idx} {...props} />
          ))}
        </div>
      </div>
    </section>
  );
}
